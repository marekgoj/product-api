# [os-dependent! but not used] export HOST_IP := $(shell ifconfig | grep -E "([0-9]{1,3}\.){3}[0-9]{1,3}" | grep -v 127.0.0.1 | awk '{ print $$2 }' | cut -f2 -d: | head -n1)

export COMPOSE_DEV_FILE = -f docker/docker-compose.yml

INTERACTIVE:=$(shell [ -t 0 ] && echo 1)

ifdef INTERACTIVE
  export COMPOSE_EXEC_OPTIONS =
else
  export COMPOSE_EXEC_OPTIONS = -T
endif
